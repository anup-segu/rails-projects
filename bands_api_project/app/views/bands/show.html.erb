<h1><%= @band.name %></h1>

<h4>All Albums</h4>
<%= render 'albums', band: @band %>

<%= button_to 'Add Album', new_band_album_url(@band), method: :get %>
<br>

<%= button_to 'Edit this Band', edit_band_url(@band), method: :get %>
<br>
<br>

<%= button_to 'Delete this band', band_url(@band), method: :delete %>
<br>

<%= link_to 'back to band index', bands_url %>
