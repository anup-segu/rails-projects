<h1><%= @album.name %></h1>

<p>
  <h4>Band: <%= @album.band.name %></h4>
</p>

<p><%= @album.ttype %> album</p>

<%= link_to "edit album", edit_album_url(@album) %>
<br>
<%= link_to "back to #{@album.band.name}", band_url(@album.band) %>
<br>
<%= link_to "back to band index", bands_url %>
